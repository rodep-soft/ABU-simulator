# ABU Roboconシミュレータの開発ルール

## 作業開始時

- 最初に `HANDOFF.md` を読み、実際のコード・README・ルール資料と照合する。会話履歴がない場合も推測で過去の実装を補わない。
- 現行アプリは `field-simulator.html` と `sim/`。`index.html`、`tactical-replay.*`、`results/` 内の配布コピーは旧版であり、対象を取り違えない。
- ユーザーの最新の依頼範囲を守る。文書だけの依頼では機能追加、修正、依存導入、移行等を一緒に行わない。
- 未解決要望を記載しただけで実装済みとしない。現在の挙動と今後の予定を明確に分ける。
- 既存の未コミット変更を勝手に破棄・整形・整理しない。不明な変更はユーザーの作業として保全する。
- Git操作前に実際のリポジトリルートと追跡状態を確認する。Windowsの元環境ではユーザーホームのGitを拾う。空のdiffをcleanやコミット済みの証拠にしない。
- commit、revert、reset、リポジトリ初期化や移行先への上書きは明示された依頼なしに行わない。

## 変更の範囲と互換性

- 既存動作を不用意に壊さない。大規模リファクタリングは必要性と影響範囲が明確な場合のみ行う。
- 既存のファイル分担、命名、CommonJS/ブラウザ両対応の構造を優先する。小さな修正のためにフレームワークやビルド基盤を追加しない。
- ブラウザと画面なし対戦は同じシミュレーション本体を使用する。高速化でルール・時間刻み・観測条件を省略しない。
- 移行時はOS固有パス、大文字小文字、文字コード、ブラウザ依存を確認する。新しい汎用処理にユーザー名付きの絶対パスを埋め込まない。
- ローカルファイルで開ける配布方式を変更する場合は、起動方法と配布への影響を明示する。
- vendorのコードとライセンスを無関係に変更・除去しない。依存追加が必要なら用途と再現可能な導入方法を記録する。

## ルール・状態・得点の正本

- 地形と配置先は `sim/field.js`、状態・合法性・実行・得点は `sim/engine.js` の `Simulation` が正本。
- GUI都合や戦略を有利にするためにゲームロジックを変更しない。表示、方策、公式ルール、シミュレーション上の仮定を分離する。
- 得点計算などのゲームルールを複数箇所に重複実装しない。特に得点は `Simulation.scores()` を再利用する。
- Earthの配置得点の所属 `placedBy` と供給元の `team`、Skyの `color` を混同しない。
- 戦略の行動はengineの検証を通す。GUI、テスト用フック、戦略から状態を直接書き換えて合法性を回避しない。
- 公式ルール未確定/未対応の動作を、推測で合法または禁止と断定しない。`SIMULATION_RULES.md` と原資料を確認し、採用した仮定を記録する。
- サンクチュアリの成立は同時完成2塔以上・うち共有1塔以上の履歴として扱い、取得後のSky反転で過去の成立を消さない。
- Mustikaを得点しても試合は180秒まで続く。終了後に未完了の動作を完成させない。
- BR順番指定の回数は成功作業後・空手で通常見渡しを終えた境界で進める。受取・補給待ち・局所再認識で進めたり、Retryで回数を初期化したりしない。順番未指定の従来方策は維持する。
- TR/BRの活動域、TR最大3個/BR最大2個、受け渡し場所、Mustika直接受渡、観測制約、Retryの採用仕様を無関係な修正で緩めない。

## GUIと終了後分析

- GUIは人間がロボット、所持物、置場、各塔の段、得点を把握できることを重視する。内部的に選択できるだけで使いやすいとは判断しない。
- GUIは現状2D俯瞰表示。L2見渡し地点と、実機視点のカメラ/FOVシミュレーションを混同しない。
- 試合終了状態と分析用状態を分離する。`RoboReview.Review` の `original` / `state` の独立したコピーと、元 `Simulation` の不変性を維持する。
- 分析によるEarth/Skyの色変更を競技中の合法行動へ転用しない。仮想盤面が実試合で到達可能だったと断定しない。
- 実終了時得点、仮想得点、差分を明確に区別する。元結果へのリセット、undo/redo、実試合/分析の別書き出しを維持する。
- 分析GUIを修正したら、SkyだけでなくEarthの各段の選択と反転、得点再計算、元ログ/再生/書き出しが変わらないことを確認する。

## BRの観測と将来のAPI

- **Simulator Ground Truth と Robot Observation は将来的に明確に分離する。** 内部の完全な盤面状態と、実機BRが取得できる情報は別物である。
- BRへ実機では取得できない情報を無条件に与えない。人間用 `snapshot()` や `window.fieldApp.sim`、相手の内部計画、未来の到着時間等をBRの入力として流用しない。
- 現行戦略の入口は `controllers.next(view)`。ロボットごとの `brain` と観測スナップショットを使い、共有グローバル状態で不可視情報を渡さない。
- BRは原則として停止・見渡し完了時に観測更新する。行動中の盤面変化を自動で見せない。失敗後の局所再観測は明示的な例外として扱う。
- L1/L2見渡し場所の使い分け、受渡後のL1観測、連続作業の観測回数を保全する。L2表示改善のために視覚制約を省略しない。
- 現在の観測には盤面全体の正確な情報など理想化が残る。コピーを渡しているだけで実機相当の知覚や安全な分離を達成したと表現しない。
- API化では観測位置/時刻、情報の古さ、未知/欠測、視野・遮蔽・認識の限界を設計し、人間/管理用情報の入口から分離する。観測外の状態がBR判断へ漏れないテストを設ける。
- BR用APIは未実装。設計制約の文書化と、API実装の完了を混同しない。

## 検証と結果の扱い

- 変更後は影響に応じて可能な範囲で実行・テストを行う。基本コマンドはプロジェクトルートで `node --test sim/tests/*.test.cjs`。
- L2/分析の重点確認は `node --test sim/tests/l2-review.test.cjs`。GUI変更は対応環境で `sim/tests/browser-smoke.cjs` / `sim/tests/l2-review-browser.cjs` と目視も利用する。
- ブラウザテストのWindows絶対パスとEdge依存はWSL移行時に別途対応する。未移植のテストを成功扱いにしない。
- 実行環境、コマンド、結果、未実行項目を区別して報告する。推測や過去の画像だけで「動作確認済み」としない。
- キャッシュ変更はキャッシュなしとの結果等価性を確認する。設定変更で古い計算や分析状態が残らないことを確認する。
- 一括対戦はまず少数試行にする。依頼なしに12,500試合や長時間の探索を開始しない。
- `results/` の既存結果・スクリーンショットは履歴として保全する。新しいコードの試行は新しい出力先へ保存する。
- `--resume` はmanifestのソースハッシュと試行範囲が同じ場合のみ。ハッシュ不一致を無視して結果を混ぜない。
- 登録戦略の総当たり、有限先読み、全行動の全探索を区別する。限られた対戦勝率を普遍的な最適戦略と呼ばない。
- 勝利・得点差を戦略評価の目的とするが、モデル内の上下界判定を実競技の無条件の勝ち確定として報告しない。

## 参照資料と引継ぎ

- 不明点は既存コード・ドキュメント・原資料を優先して確認する。矛盾や古い記述を見つけたら、その事実と未解決点を明示する。
- 上位ChatGPTプロジェクトの `sources/` 以下は同期された読み取り専用資料。編集、改名、移動、削除しない。
- ルールブック原文・日本語訳PDF、注意点PPTX、フィールドモデル(SLDPRT/STEP/STL)は `Reference/` に同梱。原資料の確認にはこの相対パスを優先し、元のWindows外部パスを必須にしない。
- WSL移行時は `Reference/` も含めてコピーし、大文字小文字を維持する。資料を無関係に加工・削除しない。日本語訳の解釈が曖昧なら原文を確認する。
- 参照資料と実行時依存を区別する。CADモデルの差替えで `sim/field.js` が自動更新される構成ではない。
- 移行、起動方法、観測境界、得点や分析仕様を変更したら `HANDOFF.md` と関連資料を更新する。
- ユーザーの作業を保全した上で、変更したもの、確認したもの、残った課題を次の担当者が追える形で残す。
