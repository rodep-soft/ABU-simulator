# 戦略の追加ガイド

## 編集するファイル

| ファイル | 役割 |
| --- | --- |
| `sim/controllers.js` | 戦略の登録一覧 `catalog`、TRの `courier`、BRの `builder`・`splitBuilder`、戦略の呼び分け `next` |
| `sim/score-planner.js` | BRの得点探索。`plan` が候補評価、`next` が帰還・受取・見渡し・作業の指示 |
| `sim/efficient-strategy.js` | 効率化版TR・BR。必要量補給、Mustika先回り、直接受取準備、短時間の補給待機 |
| `sim/match-strategies.js` | Mustika最速案・Earth固定点・L2 Earth最優先、BRの候補優先順位、150秒の方針切替、モードのTR/BR組み合わせ |
| `sim/competitive-strategies.js` | 2段目狙い、点差対応、終盤の反転順序・単体運搬。追加3モードの評価関数とSky反撃試算 |
| `sim/engine.js` | 競技ルール、採点、移動、積載制約、見渡し、時間進行。戦略の公平な比較では共通にする |
| `sim/field.js` | フィールド形状、スポット、移動先の座標 |
| `sim/view.js` | 表示と操作。通常の戦略追加では編集不要 |

**基本は「判断関数を書く → `controllers.js` の `catalog` に登録」です。**
各人の戦略には別々のIDを付けて登録すれば、赤・青の選択メニューで対戦させられます。
メニューへ追加しただけでは適用されません。ブラウザを再読み込みし、戦略を選んで
「この戦略で初期化」を押してください。TRとBRの選択は独立しています。

## 効率化版の初期設定

初期選択はTR `adaptive`、BR `efficient`。従来の `balanced`、`e3-e1s2`、`score-search` 等も比較用に残しています。
初便E3・次便E1+S2から始める効率化版はTR `adaptive-e3-e1s2` を選びます。
こちらはMustika優先へ移行すると開幕指定を短縮することがあります。従来の `e3-e1s2` は開幕2便を固定します。

- TRは観測済みの未完成タワー・在庫・BRの手持ちから必要量を求め、受け渡し容量内で最大3個を収集します。
- 条件達成まで残り1操作で必要なSkyも用意されている場合などに、最大16秒の先行移動・待機を候補とします。実際の取得は条件達成後のみです。
- BRが余ったSkyなどを持っている場合は種類別置場へ返却し、Mustikaを受け取る手を空けます。TRは条件達成済みなら不足Earthの追加採集よりMustika取得を優先します。
- Mustikaは全戦略共通で床に置かず、TRが受け渡し位置で保持し、空手のBRが `receive` で直接受け取ります。受取時間は通常把持と同じ仮設定です。受取後の見渡しを経て最優先で奉納します。
- BRの効率化評価は最大2往復の点差・時間・実際に配置する個数・持ち帰りの少なさを比較します。通常建設は2個を必須とし、Mustika単独・空手反転・出発済み作業の現地復旧は別扱いです。
- 条件達成の将来価値は、Mustikaが供給元に見えて残り時間がある場合だけ評価に加えます。これは予測得点そのものではなく、調整可能な選択用の重みです。画面の予測加点に架空の250点を足しません。
- 補給待ちは6秒、Mustikaを空手で待つ判断は18秒を目安に再判断します。ただし通常建設は待機期限後も2個必要です。2個とも置ける計画がなければ再観測、空手反転、手持ち返却を選び、1個だけで通常出発しません。末期も同様なので、残り1個で取れる得点を捨てる場合があります。
- `requirePair` は2個の通常ブロックを両方配置できる候補だけを残します。初期戦略ではEarth回収による単体運搬を候補から外します。旧 `score-search` の探索候補は変更しません。
- 配置・反転失敗後は `{ type: 'scan', local: true }` で現地停止・認識し、現在位置を起点に残り荷物を再計画します。近隣配置の完了時間が返却経路＋返却処理＋認識＋4秒を上回れば返却を選びます。この比較は調整可能な戦略上の基準です。
- 置場はEarth3個・Sky4個です。TRはBR保持分の返却空きを残します。空なら初便E3を全数納品できます。
- `RoboScorePlanner.clearCache()` は設定適用・リセット時に呼び出します。観測された障害物と移動設定でキーを分け、最大6盤面×各512経路に制限します。古い得点計画をキャッシュから使い回しません。
- TRは受取側にBRがいる間の積み増しを控え、エンジンも実際に取り出し中の列への積み増しを禁止します。観測後に相手や在庫が変化するリスクすべてをなくすものではありません。
- 最終1秒は探索の完了見積もりに余裕を取ります。ただし衝突・相手の将来行動を全探索するものではなく、最適性や全速度での勝利を保証しません。

## 2つの戦略モードを調整する

戦略画面の「戦略モード」はTRとBRの組み合わせを選ぶプリセットです。赤・青を別々に選択し、「この戦略で初期化」で適用します。TR・BRを個別変更することもできます。従来の初期選択は変更していません。

`sim/match-strategies.js` の `ranker` がBRの優先順位、`SWITCH_AT` が終盤への切替時刻（150秒）、`seedSpots` が最初の共有・専有スポットを定めます。比較は次の項目を順番に優先する方式です。

| モード・局面 | 候補の優先順位 |
| --- | --- |
| Mustika最速案・未達成 | 空いている指定共有・専有へのEarth先置き、条件の新規達成、2塔完成までの残り操作の少なさ、専有の進捗や所要時間。相手完成塔の反転も候補 |
| Earth固定点・150秒未満 | L2 Earth加点、専有Earth加点、Earth合計加点、短い時間。E1+S1の混載でもEarthはL2を優先 |
| 新規建設優先 | 配置する個数、専有への配置個数、点差増分と時間。Earth案は有用なEarth計画がない場合にSky建設、それもなければ反転へ進む |
| 150秒以降 | Sky配置・反転を含む計画、点差増分、短い時間。実行中の作業を中断せず、次の計画から切り替える |

Mustikaの取得・受取・奉納は共通の効率化手順を使います。Mustika案も奉納後は新規配置を優先し、180秒まで続行します。これは比較用の優先順位で、数学的な最速・最適解を保証するものではありません。

2モードの建設候補は、その観測からの1回の出動分を比較します。空荷の反転は `score-planner.js` の `flipBatch` が「訪問済みの組み合わせ＋最後のスポット」ごとの最短経路を比較します。最大8スポットの部分集合を扱い、同じ認識中に同じSkyを反転し直しません。見渡し場所への途中帰還なしで複数反転でき、Mustika案は条件達成後の取得に移れるよう反転列を区切ります。将来の相手行動は読まず、走行中に盤面情報も更新しません。

2モードのTRは `stock-e3` です。初便E3を固定し、その後は `efficient-strategy.js` の `stockDemand` がEarth3・Sky4への不足分を選びます。BRの返却予約分とTRの既存手持ちを数え、停止時に未採集分だけ再計画します。Earth案の150秒未満はEarth不足を優先します。これは事前設定の共有で、BRの内部計画を通信するものではありません。納品できる種類から降ろし、満杯時は受け渡し口を塞がない自陣坂の待機点へ移ります。

## L2 Earth最優先と作業回の順番

`l2-earth` は `sim/match-strategies.js` の `selectPlan` に登録した、150秒で切り替えないEarth優先方策です。`ranker` のEarth評価を使い、L2 Earth加点、専有Earth加点、Earth合計加点、短い時間の順で候補を比較します。有効なEarthを含む2個組、Earthを含む単体候補、新規配置の2個組、単体配置、最後に空手の連続反転の順です。得点と合法性は既存planner/engineを使います。

プリセットのTRは `stock-e3`。`stockDemand` はこのBR戦略の間、150秒以降もEarth不足を先に補います。在庫上限、返却予約、初便E3、Mustikaの共通優先は維持します。

GUIの「BRの順番」は、追加コードなしで既存のBR戦略を順番に呼ぶ設定です。Nodeでも同じ設定を指定できます:

```js
const { Simulation } = require('./sim/engine.js');
const controllers = require('./sim/controllers.js');
const sim = new Simulation({
  redTrPlan: 'stock-e3',
  redBrTurns: ['l2-earth', 'mustika-fast'],
  redBrPlan: 'earth-late', // 3回目以降
  blueBrTurns: ['second-layer'],
  blueBrPlan: 'endgame', // 2回目以降
});
while (!sim.ended) sim.step(0.05, controllers);
```

`Simulation.brStrategy(team)` が現在のIDを選び、`view.brPlan` に渡します。順番は固定方策ではないので通常の戦略一覧や総当たりprofileには追加していません。任意の順番の全列挙は未実装です。単体方策の既定総当たりは(TR5種 x BR10種)の赤青順序付き50 x 50 x 速度組5種 = 12,500試合です。

回数の正本はBRの `brTurn.completed` / `worked`。`recordBrTurn` は成功した `place` / `flip` / `enshrine` を記録し、空手で通常のscanを完了したときに1回進めます。受取・待機・局所scan・失敗・Retry自体では進めません。Retryでbrainを消しても回数は残り、手持ちを置く/返すまで同じ回です。何も実行できなければ、その回の方策で待機・再認識します。

複数配置・連続反転の途中で戦略を切り替えず、次回の通常見渡しを境に切り替えます。指定終了後は通常BR戦略へ戻ります。進捗はスナップショットにも含むため、リプレイ表示はその時刻の戦略になります。TRは現在のBR方針を補給選択に使いますが、相手の計画や新しい盤面情報をBRへ渡す処理は追加していません。Mustikaの共通優先処理は順番指定時にも働き、奉納も1回の成功作業に含みます。

検証は `sim/tests/l2-earth.test.cjs`, `sim/tests/br-turns.test.cjs`, `sim/tests/l2-earth-turns-browser.cjs`。新しいブラウザテストは `require('playwright')` を使い、任意の `ROBO_BROWSER_CHANNEL` と結果保存先 `ROBO_QA_DIR` に対応します。既存のWindows専用ブラウザテストとは依存解決方法が異なります。

## 追加3モードの評価

`sim/competitive-strategies.js` の `selectPlan` で候補の優先順位、`ENDGAME_AT` で終盤開始時刻を調整します。`builder` は共通の受取・認識・現地復旧・返却・Mustika手渡し手順を呼びます。TRは3モードとも `stock-e3` です。

- `second-layer`: E1+S1で完成させる既存1段塔のEarth2段目得点、既存1段塔へのEarth2段目得点、相手土台利用、固定点、時間あたり点差の順。150秒以降は従来Earth案の終盤評価へ移行。
- `score-adaptive`: 観測得点差が正なら専有加点の有無、固定点/時間、点差/時間の順。0以下なら点差/時間、Mustikaが残っていて時間余裕もある場合の条件新規達成、固定点の順。得点未観測なら固定点の建設。両局面とも通常2個で、Mustikaの実際の受取・奉納は共通手順を優先。
- `endgame`: 150秒未満はMustika案。以後は `requirePair: false` とし、1個受取・1個配置も選択肢に含め、通常の補給待ちは省く。手持ちが2個あっても時間内に片方しか置けなければ、残り荷物のある候補を許容する。受取後の帰還・認識は省かない。Mustika待ち・手渡し・奉納の既存優先は維持。

終盤の評価は、(1)Sky応答試算後も正の点差、(2)試算前の予測なら正の点差、(3)それ以外の3段階です。(1)では短時間の候補、ほかではSky応答後の参考点差を優先し、その後に予測点差、残荷物の少なさ、時間を比較します。全対戦行動の勝敗探索ではなく、比較可能なヒューリスティックです。

`replyDamage` は候補実行後の自色共有Skyについて、相手1台が巡回して減らせる点差を試算します。相手の到達時間は最後に認識した位置からの直線距離、最大速度、認識後の経過時間で短めに見積もり、認識・階段・加減速・荷物制約を課しません。操作可能距離も差し引きます。既存Skyへの最初の反転には進行中だった可能性を見込んだ時間免除を与え、各新規Skyには自分の作業完了時刻より前の反転を許しません。反転完了なら自分の減点＋相手の加点、開始だけなら終了時接触による自分の減点を数えます。

`score-planner.js` は `taskTimes` に出発計画からの各作業完了予測秒を保存します。`orderSensitive` を指定した終盤計画では、順番の違いを最短経路だけで枝刈りせず、到達可能な反転順列を比較します。既存モードは従来の部分集合・終点ごとの最短経路探索を維持します。

相手の追加建設・Mustika・TR運搬点などはこの応答試算に含みません。`assessment.skyReplyMargin` を勝利保証として使用しないでください。`assessment` はUIと判断ログに残り、通常の予測加点に仮想の得点を足しません。

## TRの運搬内容だけを変える

`controllers.js` の `catalog.TR` 配列に、次のような項目を追加します。
この例は追加用であり、共有版にはあらかじめ登録していません。

```js
{
  id: 'team-a-opening',
  name: 'Team A opening',
  shortName: 'Team A',
  opening: [
    { earth: 3, sky: 0, label: 'E3' },
    { earth: 1, sky: 2, label: 'E1+S2' },
  ],
  repeat: { earth: 2, sky: 1, label: 'E2+S1' },
  run: courier,
},
```

- `opening`: 開幕から便ごとの運搬内容。空配列なら初便から `repeat` を使います。
- `repeat`: 開幕指定の完了後に繰り返す内容。
- `earth`・`sky`: 非負整数、合計1〜3個。1便の全数を納品してから便数が進みます。
- `run: courier`: 既存の採集・移動・納品処理を再利用します。開幕指定後は条件を満たしたMustikaを優先します。
- 異なる採集順や適応的な運搬まで変えるなら、独自の判断関数を `run` に指定します。
  現在のTRメニュー・搬送目標表示は `opening` と `repeat` を前提にしています。
  独自関数を使う場合も登録に両項目を残し、表示と実際の運搬内容を一致させてください。

## BRの新しい判断を書く

`controllers.js` 内の `catalog` より前に、`myBuilder(v)` のような関数を定義し、
`catalog.BR` 配列に登録します。既存の `builder` や `splitBuilder` を参考にできます。

```js
{
  id: 'team-a-builder',
  name: 'Team A builder',
  shortName: 'Team A',
  run: myBuilder,
  details: team => [
    ['Policy', 'My building policy'],
    ['Team', team],
  ],
},
```

`name` はメニュー、`shortName` は上部の戦略表示、`details` は戦略詳細です。
`id` は同じ役割の他の戦略と重複させないでください。

## 判断関数の入出力

判断関数は、そのロボットの動作と予約された作業が空になった時に呼ばれます。
受け取る `v` はコピーで、実際の盤面を直接書き換えるものではありません。

| 入力 | 内容 |
| --- | --- |
| `v.team` / `v.role` / `v.id` | 赤・青、TR・BR、ロボットID |
| `v.x`, `v.y` | 自分の現在位置（メートル） |
| `v.cargo` | 自分が保持する物体の配列。IDだけでなく種類なども含む |
| `v.observation` | 最後に観測した盤面。未観測時は `null` |
| `v.observation.at` | 観測時刻 |
| `v.observation.stock` | 観測時の自チーム受け渡し在庫 |
| `v.observation.towers` | 観測時の自チーム専有・共有スポットの積み上げ状態 |
| `v.observation.sanctuary` | 観測時点で条件を一度でも達成していたか |
| `v.observation.source` | 供給元。BRが直接採集してよいという意味ではない |
| `v.observation.pillar` | 観測時の奉納済みMustika。なければ `null` |
| `v.observation.partner` | 観測時の味方の位置と手持ちのみ。内部計画・目的地・到着予定時刻は含まない |
| `v.observation.scores` | 観測時の両チーム得点。理想認識の追加仮定で、走行中は更新しない |
| `v.observation.opponentBR` | 観測時の相手BRの位置・高さと、既知と仮定した最高速・操作時間。内部計画や次の行動は含まない |
| `v.observation.handoff` | 観測時にTRが受け渡し位置で保持していたMustikaの受渡情報。なければ `null` |
| `v.observation.origin / local` | 認識位置 / 作業失敗後の現地認識かどうか |
| `v.observation.sanctuaryEvidence` | 達成時刻と同時完成していたタワーの個体・段・色。未達なら `null` |
| `v.time` | 現在の試合時刻。残り時間は `180 - v.time` |
| `v.motion` | 自分の速度倍率・最高速度・加速度・各動作時間など |
| `v.brain` | 前回返した判断用の記憶。開始時は `{ stage: 'start' }` |
| `v.failure` | 直前の失敗理由。なければ `null` |
| `v.transport` | 自分の納品履歴。TRの便数管理に使用 |

戻り値の形は次のとおりです。

```js
return {
  brain: { stage: 'return' },
  actions: [
    { type: 'move', target: F.points[v.team].home },
    { type: 'scan' },
  ],
};
```

これは移動と認識の指示例で、完成した建設戦略ではありません。
`brain` は毎回返します。待機なら `{ brain: v.brain, wait: 1 }` のように秒数を指定します。
JSONに保存できる値を使い、記憶に関数や実際のSimulationオブジェクトを入れないでください。

主要な作業指示:

| 指示 | 指定する値 |
| --- | --- |
| `move` | `target: { x, y }`。建設位置は `F.spotApproaches(F.spotById[id], v.team)[0]` など |
| `scan` | 通常は見渡し場所で停止・認識。配置・反転失敗後のみ `local: true` でL1/L2の現地停止可。初期値1秒 |
| `pickup` | TRのみ。`objectId` で供給元の物体を指定 |
| `unload` | TRのみ。受け渡し位置でEarth/Skyを専用列へ納品。`objectId` で手持ちを選択でき、省略時は先頭。BR保持分の返却容量を確保。Mustikaの床置きは禁止するモデル |
| `receive` | BRのみ。`objectId` で受け渡し在庫、または受け渡し位置にいる味方TRのMustikaを指定 |
| `return` | BRのみ。`objectId` のEarth/Skyを自陣TransferAreaの専用列へ返却。容量・位置・所有を確認し、加点しない |
| `place` | BRのみ。`objectId` と `spotId` を指定 |
| `flip` | BRのみ。`spotId` でSky反転を指定 |
| `recover` | BRのみ。`spotId` で自分が置いた最上段Earthの回収を指定 |
| `enshrine` | BRのみ。中央柱へMustikaを奉納 |

移動と作業は別指示です。先に到達可能な作業位置へ移動させてください。
実際の可否は実行開始時と完了時に `engine.js` が判定します。

## BRで守ること

- 判断に使う盤面は `v.observation`。走行中に `window.fieldApp.sim` などから最新状態を取得しないでください。
- 開始時・計画した一連の作業の完了後は `F.scanPoint(v.team, v)` へ戻って `scan` します。L2上なら `homeL2`、それ以外は従来のL1 `home` を返します。配置・反転失敗後は現地認識も選択できます。
- 受取・返却・Mustika直接受取の直後はL1で再認識します。移動前に計画を作る場合、見渡し先の選択には現在位置でなく受取後の `F.points[v.team].transferBR` を渡してください。
- 探索は現在位置を起点とし、補給時のL2からの下降、L1での受取後認識、作業先と同じ階への再認識帰還を時間に含めます。効率化系は有用な作業がなく待つ場合、L2を空けてL1へ戻ります。
- ブロックを受け取った後にも見渡しが必要です。最大2個を扱う作業先と順序は出発前に決定します。
- 反転だけの計画は複数箇所をまとめられます。認識時に見えていた各対象を1回ずつ扱う操作枠を持ちますが、保持数の上限は増えません。
- 在庫は上の物体から取り出します。BRは最大2個で、2個保持中のSky反転には空きがありません。
- TR・BRの活動域、供給元からの直接取得禁止、受け渡し位置などは共通エンジンに従います。
- 一連の作業が途中で不可能になった場合、後続指示は取り消されます。`v.failure.action` が `place` / `flip` なら現地停止・認識後の再計画が可能です。ほかの失敗や旧戦略は帰還・再認識してください。
- 判断用の記憶はロボット別の `v.brain` に入れます。共有変数で他ロボットの観測情報を渡さないでください。
- Mustika奉納は試合終了ではありません。180秒まで次の行動を選びます。
- 自動BRの移動停止が5秒続くとエンジンが自動Retryします。Earth/Skyは保持、Mustikaは初期柱へ返却し、`brain` は `{ stage: 'start' }`、観測は `null` に戻ります。各戦略は荷物を持った開始状態にも対応し、見渡し場所へ移動・認識してから再計画してください。この救済処理は各戦略に重複実装する必要はありません。

## 各人のファイルに分ける場合

独自の判断関数を `sim/team-a-strategy.js` などに分けることもできます。
その場合は既存の `score-planner.js` と同様の公開形式にして、
`field-simulator.html` で `controllers.js` より前に読み込み、
`controllers.js` 冒頭でブラウザ用の公開関数とNode.js用の `require` を受け取って登録します。
**ファイルをフォルダーに置くだけでは自動登録されません。**

## 動作確認

通常利用にはNode.jsは不要です。開発用テストはNode.jsの入った環境で、
このフォルダーを作業場所にして実行します。

```text
node --test sim/tests/*.test.cjs
```

既存のテストには戦略ID一覧を確認するものがあります。新規登録時はその期待値も更新し、
新しい戦略の初動・見渡し・積載・180秒終了のテストを追加してください。
ブラウザでは「戦略」で自作戦略を選択し「この戦略で初期化」した後、
「3分計算」とリプレイ・保存ログで実際の動きを確認します。

従来の得点探索は最大2往復、2モードの建設は1回の出動、空荷反転は観測済み対象の経路探索です。変更する場合も、探索範囲と評価方法を
明記し、3分全体の最適解や勝率と混同しないようにしてください。
